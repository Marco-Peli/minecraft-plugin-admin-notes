note.command.*:
    description: Player can use all /note sub-commands
    children:
        note.command.write: true
        note.command.seelast: true
        note.command.record: true
        note.command.lastbyauthor: true
        note.command.clean: true
        note.command.addmail: true
        note.command.sendmedevilnotes: true
        note.command.displaymail: true
        note.command.flagexpired: true
        note.command.reload: true
        note.command.delete: true
        note.command.notify: true
        note.command.confirm: true
        note.command.abort: true
        note.command.update: true
        note.command.flagasnotexpired: true
        note.command.help: true
        note.command.sendmenote: true
        note.command.sendmerecord: true
note.helpercommands.*:
    description: Commands suggested for helpers
    children:
        note.command.write: true
        note.command.seelast: true
        note.command.lastbyauthor: true
        note.command.notify: true
        note.command.help: true
note.modcommand.*:
    description: Commands suggested for moderators
    children:
        note.command.confirm: true
        note.command.abort: true
        note.command.addmail: true
        note.command.displaymail: true
        note.command.sendmenote: true
        note.command.sendmerecord: true
note.command.write:
    description: Player can use /note write
    default: op
note.command.seelast:
    description: Player can use /note seelast
    default: op
note.command.record:
    description: Player can use /note record
    default: op
note.command.lastbyauthor:
    description: Player can use /note lastbyauthor
    default: op
note.command.clean:
    description: Player can use /note clean
    default: op
note.command.addmail:
    description: Player can use /note addmail
    default: op
note.command.sendmenote:
    description: Player can use /note sendnote
    default: op
note.command.displaymail:
    description: Player can use /note sendnote
    default: op
note.command.flagexpired:
    description: Player can use /note flagexpired
    default: op
note.command.reload:
    descriprion: Player can use /note reload
    default: op
note.command.delete:
    description: Player can use /note delete
    default: op
note.command.notify:
    description: Player is notified when a player with more than [notes_per_notify_threshold] notes, join
    default: op
note.command.flagasnotexpired:
    description: Player can use /note flagasnotexpired
    default: op
note.command.confirm:
    description: Player can use /note confirm
    default: op
note.command.abort:
    description: Player can use /note abort
    default: op
note.command.update:
    description: Currently not implemented
    default: op
note.command.help:
    description: Player can use /note help
    default: op